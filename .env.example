EFS_FILE_DIRE=
SPRING_DATASOURCE_URL=
SPRING_DATASOURCE_USERNAME=
SPRING_DATASOURCE_PASSWORD=
SPRING_KAFKA_BOOTSTRAP_SERVERS=
WORKER_CALLBACK_TOKEN=
# One AWS identity for SES mail and the platform buckets (etl-avatar, etl-config).
# Leave the keys blank to sign with the instance role; set the endpoint only for LocalStack.
AWS_ENDPOINT=
AWS_REGION=
AWS_ACCESS_KEY=
AWS_SECRET_KEY=
